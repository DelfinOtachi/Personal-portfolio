services:
  - type: web
    name: php-static-site
    env: php
    buildCommand: ""  # No build step for a static PHP site
    startCommand: ""  # Render will use PHP's built-in server to serve your PHP files
